export namespace ExperimentFeaturePlotDataPartResponse {
    export interface Response {
        x ? : (string)[] | null;
        y ? : (string)[] | null;
        z ? : ((number)[] | null)[] | null;
    }
}