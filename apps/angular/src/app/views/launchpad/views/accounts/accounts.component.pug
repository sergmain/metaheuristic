.section
    .section__header
        ct-section-caption Experiments
    .section__body
        ct-table.simple-table(#table)
            table(mat-table '[dataSource]'="dataSource" class="mat-table") 
                ng-container(matColumnDef="id") 
                    th(mat-header-cell *matHeaderCellDef) ID 
                    td(mat-cell *matCellDef="let el") {{el.id}} 
                ng-container(matColumnDef="isEnabled") 
                    th(mat-header-cell *matHeaderCellDef) Is enabled 
                    td(mat-cell *matCellDef="let el") {{el.enabled ? 'Yes':'No'}} 
                ng-container(matColumnDef="login") 
                    th(mat-header-cell *matHeaderCellDef) Login 
                    td(mat-cell *matCellDef="let el") {{el.login}} 
                ng-container(matColumnDef="publicName") 
                    th(mat-header-cell *matHeaderCellDef) Public name 
                    td(mat-cell *matCellDef="let el") {{el.publicName}} 
                ng-container(matColumnDef="createdOn") 
                    th(mat-header-cell *matHeaderCellDef) Created On 
                    td(mat-cell *matCellDef="let el") {{el.createdOn | date:'HH:mm:ss, MMM&nbsp;d,&nbsp;yyyy'}}   

                ng-container(matColumnDef="bts") 
                    th(mat-header-cell *matHeaderCellDef)  
                    td(mat-cell *matCellDef="let el") 
                        .buttons.buttons--right 
                            button(mat-flat-button [routerLink]="['/launchpad/accounts/edit/',el.id]" color="primary")  Edit
                            button(mat-flat-button [routerLink]="['/launchpad/accounts/edit-password/',el.id]" color="primary")   Change Password
                tr(mat-header-row *matHeaderRowDef="columnsToDisplay")  
                tr(mat-row *matRowDef="let row; columns: columnsToDisplay;") 
        .section__content('*ngIf'="currentStates.has(states.firstLoading)")
            mat-progress-bar(mode="indeterminate")

    .section__footer
        .flex.flex--jc_sb
            .buttons.buttons--left 
                button(mat-flat-button #prevTable disabled '(click)'='prev()') <mat-icon>arrow_back_ios</mat-icon>
                button(mat-flat-button #nextTable disabled '(click)'='next()') <mat-icon>arrow_forward_ios</mat-icon>
            .buttons.buttons--right
                button(mat-flat-button color="primary"
                    '[disabled]'="currentStates.has(states.loading) || currentStates.has(states.firstLoading)" 
                    routerLink="/launchpad/accounts/add/") Add Account 



