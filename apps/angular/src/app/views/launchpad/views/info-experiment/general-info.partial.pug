.simple-table
    table(mat-table '[dataSource]'="tables.generalInfo.table") 
        ng-container(matColumnDef="key") 
            th(mat-header-cell *matHeaderCellDef) Key
            td(mat-cell *matCellDef="let el") {{el[0]}}
        ng-container(matColumnDef="value") 
            th(mat-header-cell *matHeaderCellDef) Value
            td(mat-cell *matCellDef="let el") {{el[1]}}
        tr(mat-header-row *matHeaderRowDef="tables.generalInfo.columnsToDisplay") 
        tr(mat-row *matRowDef="let row; columns: tables.generalInfo.columnsToDisplay;") 